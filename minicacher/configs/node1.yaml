node:
  id: "node1"
  bind_addr: "localhost"
  data_dir: "./data/node1"

http:
  port: 11000

raft:
  port: 12000

cluster:
  join_addr: "" # Empty for bootstrap node

logging:
  level: "info"
  json: false
