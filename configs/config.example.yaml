# Distributed Cache Configuration Example

node:
  # Unique identifier for this node
  # If not set, defaults to the Raft bind address
  id: "node1"

  # Hostname or IP address shared by the HTTP and Raft listeners
  bind_addr: "localhost"

  # Directory for Raft data storage
  data_dir: "./data"

http:
  # HTTP API port
  port: 11000

raft:
  # Raft consensus port
  port: 12000

cluster:
  # Address of existing node to join (empty for bootstrap node)
  join_addr: ""

logging:
  # Log level: debug, info, warn, error
  level: "info"

  # Output logs in JSON format
  json: false
